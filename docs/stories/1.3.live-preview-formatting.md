# Story 1.3: Live Preview & Conventional Commit Formatting

**Epic:** 1 — Foundation & Commit Generator Core  
**Status:** Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["npm test", "npm run lint"]
```

## Story

**As a** user,  
**I want** to see the formatted commit message update as I type,  
**so that** I know exactly what will be copied.

## Acceptance Criteria

1. Preview exibe `type(scope): description` ou `type: description` conforme FR4.
2. Atualização em tempo real sem reload (FR10).
3. Função pura `buildCommitMessage(type, scope, description)` coberta por testes unitários.
4. Aviso não bloqueante se descrição começar com maiúscula ou terminar com ponto.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## Tasks / Subtasks

- [ ] Domínio `buildCommitMessage` (AC: 1, 3, 4)
  - [ ] `src/lib/buildCommitMessage.ts`
  - [ ] `buildCommitMessage.test.ts` — com/sem escopo, warnings
- [ ] Hook `useCommitForm` (AC: 2)
  - [ ] Estado formulário + preview derivado via `useMemo`
  - [ ] Chamar `validateCommitForm` + `buildCommitMessage`
- [ ] Componente `CommitPreview` (AC: 1, 2)
  - [ ] `font-mono`, fundo elevated, `user-select: all`
  - [ ] Lista de warnings não bloqueantes
  - [ ] `aria-label="Preview da mensagem de commit"`
- [ ] Layout desktop 50/50 form + preview [Source: front-end-spec §5]

## Dev Notes

### buildCommitMessage [Source: docs/architecture.md#4.2]

```typescript
// Com escopo: `${type}(${scope}): ${description}`
// Sem escopo: `${type}: ${description}`
// warnings: maiúscula inicial, ponto final
```

### Performance

- Preview síncrono, &lt; 16ms — sem debounce necessário

### Dependência

- **Requer Story 1.2 Done** (`CommitForm`, types)

### Testing

- 100% cobertura em `src/lib/buildCommitMessage.ts`
- RTL: digitar no form → preview atualiza

## Change Log

| Date       | Version | Description  | Author |
|------------|---------|--------------|--------|
| 2026-05-27 | 1.0     | Story criada | River  |

## Dev Agent Record

_(preencher pelo @dev)_

## QA Results

_(preencher pelo @qa)_
