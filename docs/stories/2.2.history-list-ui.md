# Story 2.2: History List UI — Reuse & Recopy

**Epic:** 2 — History & UX Polish  
**Status:** Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: ["npm test", "keyboard navigation check"]
```

## Story

**As a** user,  
**I want** to browse and reuse past commit messages,  
**so that** I can speed up similar commits.

## Acceptance Criteria

1. Lista ordenada por data decrescente (FR7).
2. Ação "Reutilizar" preenche o formulário com os campos salvos.
3. Ação "Copiar" em cada item do histórico.
4. Estado vazio amigável quando não há histórico.
5. Seção acessível por teclado e leitores de tela.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## Tasks / Subtasks

- [x] `HistoryList` + `HistoryItem` (AC: 1, 4, 5)
  - [x] Ordenar `createdAt` desc
  - [x] Timestamp relativo ("há 2 min")
  - [x] Estado vazio com mensagem
  - [x] Seção `#historico` com heading H2
- [x] Ações (AC: 2, 3)
  - [x] Reutilizar → callback preenche `useCommitForm` + scroll suave ao topo
  - [x] Copiar por item via `useClipboard`
- [x] Header histórico com link âncora [Source: front-end-spec §3]
- [x] Testes RTL para lista vazia e com itens

## Dev Notes

### UI [Source: docs/front-end-spec.md#5]

- Cards com mensagem truncada; botões secondary Reutilizar/Copiar
- Desktop: grid 2 colunas

### Dependência

- **Requer Story 2.1 Done**

## Change Log

| Date       | Version | Description  | Author |
|------------|---------|--------------|--------|
| 2026-05-27 | 1.0     | Story criada | River  |
| 2026-05-27 | 1.1     | Implemented — Status: Draft → Review | @dev |

## Dev Agent Record

### Agent Model Used

Composer (Dex @dev)

### Completion Notes

- `HistoryList` com grid 2 colunas (lg), estado vazio ilustrado e lista acessível (`ul` + `aria-label`).
- `HistoryItem` com mensagem truncada, `<time>`, botões Reutilizar/Copiar (secondary) e feedback por item.
- `formatRelativeTime` para timestamps relativos em pt-BR.
- Reutilizar preenche formulário via `setInputValues` e faz scroll suave ao topo.

### File List

| Arquivo | Ação |
|---------|------|
| `src/lib/formatRelativeTime.ts` | Criado |
| `src/lib/formatRelativeTime.test.ts` | Criado |
| `src/components/HistoryList.tsx` | Criado |
| `src/components/HistoryList.test.tsx` | Criado |
| `src/components/HistoryItem.tsx` | Criado |
| `src/components/HistoryItem.test.tsx` | Criado |
| `src/App.tsx` | Modificado |
| `src/App.test.tsx` | Modificado |

## QA Results

### Review Date: 2026-05-27

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

UI alinhada à spec: cards, grid 2 colunas, timestamps relativos, ações secundárias. Reutilizar integrado corretamente com `setInputValues` e scroll.

### Refactoring Performed

Nenhum — código aprovado sem alterações.

### Compliance Check

- Coding Standards: ✓
- Project Structure: ✓
- Testing Strategy: ✓
- All ACs Met: ✓

### Requirements Traceability

| AC | Given-When-Then | Evidência |
|----|-----------------|-----------|
| 1 | Dado múltiplas entradas, quando renderizar lista, então ordem desc por data | `HistoryList.test` sort |
| 2 | Dado item no histórico, quando Reutilizar, então formulário preenchido | `App.test` reuses history |
| 3 | Dado item, quando Copiar, então clipboard recebe mensagem | `HistoryItem.test` |
| 4 | Dado histórico vazio, quando abrir seção, então estado vazio amigável | `HistoryList.test` + `App.test` |
| 5 | Dado lista, quando navegar por teclado, então botões focáveis com labels | Estrutura semântica `ul`/`time`/aria-labels (manual keyboard check recomendado) |

### Improvements Checklist

- [ ] Teste automatizado de navegação por teclado (tech debt)
- [x] Copiar do histórico não duplica entrada (comportamento correto)

### Gate Status

Gate: **PASS** → `docs/qa/gates/2.2-history-list-ui.yml`

### Recommended Status

✓ Ready for Done
