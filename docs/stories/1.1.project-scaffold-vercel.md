# Story 1.1: Project Scaffold & Vercel Deploy Pipeline

**Epic:** 1 — Foundation & Commit Generator Core  
**Status:** Ready

## Executor Assignment

```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["npm run lint", "npm run typecheck", "npm test", "npm run build"]
```

## Story

**As a** developer,  
**I want** a configured React/Vite/TypeScript project with Vercel deployment,  
**so that** the team has a reproducible base to build features.

## Acceptance Criteria

1. Repositório contém app Vite + React + TypeScript + Tailwind configurados.
2. Scripts `dev`, `build`, `lint`, `test`, `typecheck` definidos no `package.json`.
3. `vercel.json` ou configuração equivalente para build estático.
4. Deploy de preview na Vercel funciona a partir do branch `main`.
5. README documenta comandos locais e URL de produção.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled  
> Quality validation via manual review and CI (`npm run lint`, `npm test`, `npm run build`).

## Tasks / Subtasks

- [ ] Scaffold Vite React TS na raiz do projeto (AC: 1)
  - [ ] Usar `npm create vite@latest . -- --template react-ts` **no diretório existente** (preservar `.aiox-core/`, `docs/`, `.aiox/`)
  - [ ] Mesclar/substituir `package.json` placeholder atual pelos scripts reais do Vite
  - [ ] Instalar e configurar Tailwind CSS 4
  - [ ] Configurar alias `@/` → `src/` em `vite.config.ts` e `tsconfig`
  - [ ] Criar pastas vazias: `src/components`, `src/hooks`, `src/lib`, `src/types`
- [ ] Scripts e quality tools (AC: 2)
  - [ ] ESLint 9 + typescript-eslint
  - [ ] Vitest + jsdom + Testing Library
  - [ ] Scripts: `dev`, `build`, `lint`, `typecheck`, `test`
- [ ] Deploy Vercel (AC: 3, 4)
  - [ ] Criar `vercel.json` (`buildCommand`, `outputDirectory: dist`)
  - [ ] **Ação humana:** conectar repo `Cauams19/atividade-aula-09` no dashboard Vercel (se ainda não conectado)
  - [ ] Validar deploy production em `main` e preview em PR
- [ ] CI GitHub Actions (AC: 2, 4)
  - [ ] Workflow `.github/workflows/ci.yml`: ci, lint, typecheck, test, build
- [ ] Documentação (AC: 5)
  - [ ] Atualizar README com setup, scripts e URL Vercel

## Dev Notes

### Stack [Source: docs/architecture/pilha-tecnologica.md]

- React 19, Vite 6, TypeScript 5, Tailwind 4, Vitest 3, Node 22

### Estrutura alvo [Source: docs/architecture/arvore-de-origem.md]

```
src/
├── components/
├── hooks/
├── lib/
├── types/
├── App.tsx
└── main.tsx
```

### Vercel config [Source: docs/architecture.md#7]

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
```

### Testing

- Vitest config em `vitest.config.ts`; smoke test placeholder em `src/App.test.tsx`
- CI deve passar antes de marcar Done

### Contexto do repositório (brownfield-in-greenfield)

- Repo já possui bootstrap AIOX, `docs/`, `.aiox-core/` e `package.json` placeholder — **não** inicializar projeto em subpasta separada
- Git remote: `https://github.com/Cauams19/atividade-aula-09.git`

## PO Validation Notes

**Validated:** 2026-05-27 by @po (Pax)  
**Score:** 8.5/10 · **Verdict:** GO  
**Adjustments applied:** scaffold em repo existente, ação humana Vercel, pastas `src/*`

## Change Log

| Date       | Version | Description     | Author |
|------------|---------|-----------------|--------|
| 2026-05-27 | 1.0     | Story criada    | River  |
| 2026-05-27 | 1.1     | Validated GO (8.5/10) — Status: Draft → Ready | @po |

## Dev Agent Record

### Agent Model Used

_(preencher pelo @dev)_

### Completion Notes

_(preencher pelo @dev)_

### File List

_(preencher pelo @dev)_

## QA Results

_(preencher pelo @qa)_
